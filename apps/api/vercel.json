{
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile",
  "buildCommand": "echo 'Vercel compiles TypeScript serverless functions automatically'",
  "rewrites": [{ "source": "/(.*)", "destination": "/api" }]
}
